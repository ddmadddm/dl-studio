import { Pass, PassUsage } from '@/types/pass';

/**
 * 이용권 시드 데이터 — 리셋됨 (빈 상태에서 직접 입력)
 * 필요 시 이용권관리 화면의 "이용권 추가"로 등록한다.
 */
export const mockPasses: Pass[] = [];
export const mockPassUsages: PassUsage[] = [];
